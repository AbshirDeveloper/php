/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AmFilPipe } from './am-fil.pipe';

describe('AmFilPipe', () => {
  it('create an instance', () => {
    const pipe = new AmFilPipe();
    expect(pipe).toBeTruthy();
  });
});
