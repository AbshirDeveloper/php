/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FiltPipe } from './filt.pipe';

describe('FiltPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltPipe();
    expect(pipe).toBeTruthy();
  });
});
