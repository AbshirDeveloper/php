/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EntryPipe } from './entry.pipe';

describe('EntryPipe', () => {
  it('create an instance', () => {
    const pipe = new EntryPipe();
    expect(pipe).toBeTruthy();
  });
});
